apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "urthehero.fullname" . }}-test-connection"
  labels:
{{ include "urthehero.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "urthehero.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
